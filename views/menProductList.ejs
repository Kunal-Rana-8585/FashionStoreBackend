<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Men's Products List</title>
  <style>
    body {
      font-family: sans-serif;
      margin: 40px;
    }

    h1 {
      text-align: center;
      margin-bottom: 30px;
      font-size: 24px;
      color: #333;
    }

    ul {
      list-style: none;
      padding: 0;
      display: flex;
      flex-wrap: wrap; /* Enable wrapping for multi-column layout */
      justify-content: space-around; /* Even spacing between items */
    }

    li {
      width: 300px; /* Adjust width as needed */
      margin-bottom: 30px;
      border: 1px solid #ccc;
      border-radius: 5px;
      padding: 15px;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
    }



    li strong {
      font-size: 18px;
      display: block;
      margin-bottom: 5px;
    }

    li p {
      font-size: 14px;
      color: #666;
    }

    li form {
      display: flex;
      justify-content: space-between;
      margin-top: 10px;
    }

    li button {
      background-color: #eee;
      border: none;
      padding: 5px 10px;
      border-radius: 3px;
      cursor: pointer;
      font-size: 12px;
    }

    li button:hover {
      background-color: #ddd;
    }

    a {
      text-decoration: none;
      color: #333;
      font-weight: bold;
      display: block;
      margin-top: 20px;
      text-align: center;
    }
  </style>
</head>
<body>
  <h1>Men's Products</h1>
  <ul>
    <% products.forEach(product => { %>
      <li>
        <strong><%= product.heading %></strong>
        <p>Price: $<%= product.price %></p>
        <p>Image: <%= product.image %></p>
        <form action="/menproducts/edit/<%= product.id %>" method="GET" style="display:inline;">
          <button type="submit">Edit</button>
        </form>
        <form action="/menproducts/delete/<%= product.id %>" method="POST" style="display:inline;">
          <button type="submit">Delete</button>
        </form>
      </li>
    <% }); %>
  </ul>

  <a href="/menproducts/add">Add New Product</a>
</body>
</html>
